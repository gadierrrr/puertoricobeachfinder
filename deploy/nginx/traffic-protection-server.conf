# Include in the canonical HTTPS server, alongside its existing access log.
# Empty map keys exempt beaches, assets, APIs, and OAuth callbacks.
limit_req zone=bf_login burst=20 nodelay;
limit_req_status 429;
limit_req_log_level notice;
access_log /var/log/nginx/puertoricobeachfinder-traffic.log bf_traffic;
